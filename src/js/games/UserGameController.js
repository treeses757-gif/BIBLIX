// ========== FILE: src/js/games/UserGameController.js ==========
// Контроллер мультиплеера (упрощён)
export class UserGameController {
  constructor(rtdb, auth) {
    this.rtdb = rtdb;
    this.auth = auth;
  }
  
  // Методы будут расширены в Matchmaker
}